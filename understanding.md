# 이 프로젝트에 대한 나의 이해

> 한 문장 요약: **값비싼 실세계 데이터를 값싼 가상 데이터가 (대부분) 대체할 수 있는가?**
> — 대답은 "그렇다"이고, 이 프로젝트는 그 정도를 수치로 측정했다.

## 1. 배경 — 왜 이런 질문이 나오는가

평소의 머신러닝은 충분히 검증된 대량의 실데이터를 전제로 한다. 데이터가 부족하면
더 모으면 되고, 품질이 나쁜 데이터는 폐기하면 된다.

반도체 fab에서는 이 전제가 무너진다:

- **웨이퍼 한 장이 고가**이고, 데이터 한 줄을 얻으려면 수십 단계의 값비싼
  공정 + 측정(EDS)을 실제로 통과시켜야 한다.
- 특히 **다양한 공정 조건**의 데이터가 필요하다 — 그런데 조건을 바꿔가며
  양산 라인을 돌리는 것은 장수를 늘리는 것보다 훨씬 더 비싸다.
- 그래서 "라벨된 실데이터를 무한히 모은다"는 선택지가 애초에 없다.

여기서 나오는 아이디어: **공정 물리를 아는 만큼 시뮬레이터(디지털 트윈)로
가상 웨이퍼를 무한히 찍어내고, 그것으로 EDS 양불 판정 모델을 학습시키면 되지
않을까?** 이 프로젝트는 그 아이디어가 실제로 성립하는지를 처음부터 끝까지
직접 만들어서 확인한 것이다.

## 2. 핵심 뉘앙스 — 가상 데이터의 "불완전함"이란 무엇인가

처음에 나는 가상 데이터를 "불안정한(불완전한) 데이터"라고 이해했는데,
정확한 그림은 조금 다르다.

- 흔히 폐기 대상이 되는 불완전한 데이터는 **샘플 단위의 오염**이다 —
  라벨 오류, 결측치, 이상치 같은 것.
- 이 프로젝트의 가상 데이터는 샘플 하나하나는 깨끗하고 내부적으로 일관된다.
  문제는 **데이터를 만들어낸 세계 자체가 현실과 체계적으로 어긋나 있다**는
  점이다. 시뮬레이터가 아무리 정교해도 현실의 물리를 100% 재현할 수는 없기
  때문이다.

코드에서는 이것을 일부러 명시적으로 구현했다
(`simulation/config.py`):

- 디지털 트윈의 물리 상수 `PHYSICS`와 현실 fab의 `REAL_FAB_PHYSICS`를
  **다르게** 설정 (예: Oxide→Vth 결합 0.010 vs 0.013). 트윈은 이 차이를
  모른다.
- 현실 fab의 측정 노이즈 `REAL_FAB_NOISE`는 트윈보다 **약 40% 더 크게**
  설정 — 현실의 테스터가 더 지저분하다는 것까지 흉내냈다.

즉 이 실험의 가상 데이터는 "노이즈 낀 데이터"가 아니라
**"틀린 물리 법칙에서 태어난, 체계적으로 편향된 분포"**다.
그런데도 쓸모가 있는가? — 이것이 진짜 질문이다.

## 3. 실험 설계 (sim2real, `ml/sim2real.py`)

두 개의 세계를 만들었다:

| | 디지털 트윈 (가상 fab) | 현실 fab |
|---|---|---|
| 데이터 위치 | `graph/run_*` | `graph/real/run_*` (`main.py --real`) |
| 물리 상수 | `PHYSICS` | `REAL_FAB_PHYSICS` (어긋남) |
| 측정 노이즈 | `MEASUREMENT_NOISE` | `REAL_FAB_NOISE` (~40% ↑) |
| 스펙 한계 | 공통 (제품 요구사항이므로 동일) | 공통 |
| 데이터 비용 | 공짜 (조건 12가지 × 2만 장) | 비쌈 (예산 제한) |

현실 fab의 EDS 모델을 만드는 세 가지 전략을, 현실 fab의 held-out run
2개(40,000장)로 채점했다:

1. **scratch** — 살 수 있는 만큼의 실데이터만으로 처음부터 학습
2. **zero-shot** — 가상 데이터만으로 학습해서 그대로 투입 (실데이터 0장)
3. **fine-tune** — 가상 데이터로 사전학습 후, 소량의 실데이터로 미세조정

실데이터 예산을 250 / 1,000 / 4,000 / 16,000장으로 바꿔가며 비교했다.

## 4. 결과

F1 (fail 클래스, 3 seed 평균):

| 전략 | 250장 | 1,000장 | 4,000장 | 16,000장 |
|---|---|---|---|---|
| scratch (실데이터만) | 57.44% | 69.88% | 76.15% | 77.45% |
| **fine-tune (가상 사전학습 + 실데이터)** | **78.61%** | **78.64%** | **79.37%** | **79.27%** |

기준선: zero-shot F1 **77.13%**, oracle(실데이터 20,000장 전부 사용) **76.56%**.

읽는 법:

- **실데이터 250장 + 가상 사전학습이, 실데이터 20,000장 scratch(oracle)보다
  낫다** (78.6% > 76.6%). 실데이터 요구량이 사실상 **80분의 1**로 줄었다.
- **zero-shot조차 77%** — 물리가 어긋난 세계에서 학습했는데도, 실데이터를
  한 장도 안 보고 oracle을 이긴다. 가상 데이터의 가치는 물리적 완벽함에
  의존하지 않는다.
- **fine-tune은 zero-shot보다 전 구간 우위 (+1.5~2.2%p)** — 물리가 어긋난
  만큼의 손해는, 소량의 실데이터 적응으로 메워진다.

## 5. 왜 250장이 20,000장을 이기는가 — 양이 아니라 다양성

이 결과의 핵심 이유는 데이터의 **양**이 아니라 **공정 조건의 다양성**이다.

- 실데이터 pool 20,000장은 전부 **단 하나의 공정 조건**(run 1개)에서 나왔다.
  같은 조건의 웨이퍼를 아무리 많이 봐도, 모델은 그 조건 근처의 세계밖에
  모른다.
- 반면 트윈은 **12가지 공정 조건**(run마다 중심·산포가 다르게 샘플링됨)을
  공짜로 휩쓸었다. 스펙 박스의 모양을 훨씬 넓은 영역에서 배운 것이다.

이것이 현실 fab에서 가상 데이터를 쓰는 진짜 이유와 정확히 일치한다:
**데이터 수집의 병목은 웨이퍼 장수가 아니라 조건 커버리지**이고,
조건을 바꿔가며 실험하는 비용이야말로 시뮬레이션이 대체해 주는 부분이다.

## 6. 이 프로젝트가 말하는 것 / 말하지 않는 것

**말하는 것:**

> 가상 데이터의 가치는 물리적 완벽함에서 오지 않는다. 시뮬레이터가 현실과
> 조금 어긋나 있어도 그 가치는 살아남으며, 어긋난 만큼은 소량의 실데이터
> fine-tune으로 메울 수 있다. 따라서 값싼 가상 데이터가 값비싼 실데이터의
> 대부분을 대체할 수 있다.

**말하지 않는 것 (내가 처음에 헷갈렸던 부분):**

- "가상 데이터가 현실의 물리 법칙에 위배되지 않는지 검증한다"는 방향이
  **아니다**. 오히려 이 실험은 트윈의 물리를 **일부러 틀리게** 만들어 놓고,
  그래도 쓸모가 남는지를 본 것이다. 검증이 아니라 **강건성(robustness)
  실험**이다.
- 실데이터로 시뮬레이터 자체를 고치는 것(물리 상수를 역추정하는
  **디지털 트윈 캘리브레이션**)은 방향이 반대인 별개의 문제다.
  "모델을 현실에 적응시키기(fine-tune)"와 "시뮬레이터를 현실에 맞추기
  (calibration)" 중 무엇이 효율적인가 — 는 이 프로젝트의 다음 장이 될 수
  있는 열린 질문이다.

## 7. 전체 스토리에서의 위치

이 프로젝트는 세 층이 하나의 서사로 연결된다:

1. **가상 fab** (`simulation/`) — 물리 결합이 반영된 웨이퍼 데이터를 값싸게
   찍어내는 시뮬레이터. 라벨은 참값 기준, 저장되는 특징은 측정값(노이즈 포함)
   — 그래서 어떤 모델도 100%에 도달할 수 없는 진짜 확률적 문제가 된다.
2. **밑바닥 ML** (`ml/`) — numpy만으로 구현한 퍼셉트론과 MLP. 양품 영역이
   스펙 박스(구간 AND)라 단층 퍼셉트론은 구조적으로 못 풀고 MLP는 푼다는
   것을 직접 확인.
3. **sim2real 실험** — 위의 두 층을 붙여서 "이 가상 데이터, 진짜 쓸모
   있는가?"라는 실용적 질문에 정량적으로 답함 (실데이터 80배 절감).

즉 "시뮬레이터 장난감 + ML 연습"이 아니라, **가상 데이터 기반 EDS 양불
판정이라는 하나의 질문을 세운 뒤 끝까지 검증한 프로젝트**다.
